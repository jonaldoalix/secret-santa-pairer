services:
  secret-santa-pairer:
    build: .
    container_name: secret-santa-pairer
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PORT: "3024"
      MUSEUM_MODE: "true"
      NOTIFY_PROVIDER: "stub"
      SEED_MUSEUM_DEMO: "true"
    ports:
      - "3024:3024"
